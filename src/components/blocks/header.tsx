import React from 'react';
import Navigation from "@/components/widgets/navigation";

const Header = () => {
    return (
        <div>
            <Navigation />
        </div>
    );
};

export default Header;